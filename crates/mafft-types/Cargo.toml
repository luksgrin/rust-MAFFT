[package]
name = "mafft-types"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Pure Rust types for MAFFT data structures"

[features]
ffi = ["dep:mafft-sys"]

[dependencies]
mafft-sys = { path = "../mafft-sys", optional = true }
num-complex = "0.4"
