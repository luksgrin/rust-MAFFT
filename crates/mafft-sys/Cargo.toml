[package]
name = "mafft-sys"
version = "0.0.1"
edition.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
authors.workspace = true
keywords.workspace = true
categories.workspace = true
description = "Reserved namespace for future raw FFI bindings to MAFFT C source (currently a stub)"
readme = "README.md"

# Intentionally pinned at 0.0.1 (independent of the workspace version) so
# this stub stays distinguishable from any future real `mafft-sys`
# release. Once vendored C source is added and real `unsafe extern "C"`
# bindings are exposed, bump to 0.1.0+ and update `description`.

[lib]
name = "mafft_sys"
path = "src/lib.rs"
